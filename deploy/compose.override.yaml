services:
  postgres:
    ports:
      - "5432:5432"

  prometheus:
    ports:
      - "9090:9090"

  grafana:
    ports:
      - "3000:3000"

  app:
    environment:
      BASE_URL: "http://localhost:8080"
